# ❌ `tokens.py`를 깃허브에서 제외 (중요!!)
tokens.py

# Python 관련 불필요한 파일 무시
__pycache__/
*.pyc
*.pyo
*.pyd

# 가상 환경 폴더 무시 (가상 환경 이름에 따라 변경 가능)
venv/
env/

# 로그 파일 무시
*.log

# 특정 폴더 제외
temp/

# JSON 파일 포함 (보안상 민감한 JSON이 있다면 제외 가능)
# study_data.json을 제외하려면 아래 주석 해제
# study_data.json
